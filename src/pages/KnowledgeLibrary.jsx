import React, { useState } from 'react';
import { BookOpenCheck, CheckCircle2, ExternalLink, FileText, Search } from 'lucide-react';
import BackButton from '../components/BackButton';
import { useApp } from '../context/AppContext';
import { knowledgeDocuments } from '../data/knowledgeDocuments';

export default function KnowledgeLibrary() {
  const { userProgress, markDocumentRead } = useApp();
  const [query, setQuery] = useState('');
  const readDocuments = userProgress.readDocuments || {};
  const filtered = knowledgeDocuments.filter((document) => `${document.title} ${document.category} ${document.summary}`.includes(query));
  return <div className="mx-auto max-w-6xl" dir="rtl"><div className="mb-7 flex items-start justify-between gap-4"><div><span className="mb-2 inline-flex rounded-full border border-[#00e6ff]/20 bg-[#00e6ff]/5 px-3 py-1 text-[10px] font-black text-[#00e6ff]">13 חומרי לימוד מקצועיים</span><h1 className="text-3xl font-black text-white">ספריית חומרי הלימוד</h1><p className="mt-2 text-sm text-gray-500">מצגות מקצועיות בניהול סייבר, סיכונים, מודיעין והמשכיות עסקית</p></div><BackButton /></div><label className="mb-6 flex items-center gap-3 rounded-2xl border border-gray-800 bg-gray-900/55 px-4 py-3"><Search size={18} className="text-gray-600" /><input value={query} onChange={(event) => setQuery(event.target.value)} className="w-full bg-transparent text-sm text-white outline-none" placeholder="חיפוש בחומרי הלימוד…" /></label><div className="grid gap-5 md:grid-cols-2 xl:grid-cols-3">{filtered.map((document) => { const read = readDocuments[document.id]; return <article key={document.id} className={`group overflow-hidden rounded-3xl border transition ${read ? 'border-emerald-500/20 bg-emerald-500/5' : 'border-gray-800 bg-gray-900/45 hover:border-[#00e6ff]/25'}`}><a href={document.url} target="_blank" rel="noreferrer" className="relative block aspect-[16/9] overflow-hidden bg-gray-950"><img src={document.thumbnail} alt={`כריכת ${document.title}`} className="h-full w-full object-cover transition duration-300 group-hover:scale-[1.025]" /><span className="absolute left-3 top-3 rounded-lg border border-white/10 bg-black/70 px-2.5 py-1 text-[9px] font-black text-white backdrop-blur">{document.pages} עמודים</span></a><div className="p-5"><div className="flex items-start justify-between gap-3"><span className={`grid h-10 w-10 shrink-0 place-items-center rounded-xl ${read ? 'bg-emerald-500/10 text-emerald-400' : 'bg-gray-950 text-[#00e6ff]'}`}>{read ? <BookOpenCheck size={20} /> : <FileText size={20} />}</span><span className="text-left"><span className="block rounded-lg bg-gray-950 px-2.5 py-1 text-[9px] font-black text-gray-500">{document.type}</span><small className="mt-1.5 block text-[9px] font-bold text-gray-600">{document.category}</small></span></div><h2 className="mt-4 min-h-12 text-base font-black leading-6 text-white">{document.title}</h2><p className="mt-2 min-h-16 text-xs font-semibold leading-6 text-gray-500">{document.summary}</p><div className="mt-5 grid grid-cols-2 gap-2"><a href={document.url} target="_blank" rel="noreferrer" className="flex items-center justify-center gap-2 rounded-xl border border-[#00e6ff]/25 bg-[#00e6ff]/8 py-2.5 text-xs font-black text-[#63f2ff]"><ExternalLink size={14} /> פתיחת החומר</a><button type="button" onClick={() => markDocumentRead(document.id)} disabled={Boolean(read)} className={`flex items-center justify-center gap-2 rounded-xl py-2.5 text-xs font-black ${read ? 'cursor-default bg-emerald-500/10 text-emerald-400' : 'bg-[#00e6ff] text-black'}`}><CheckCircle2 size={14} />{read ? 'קראתי' : 'סימון קראתי'}</button></div>{read && <p className="mt-3 text-center text-[9px] font-bold text-emerald-500">נקרא בתאריך {new Intl.DateTimeFormat('he-IL', { dateStyle: 'short' }).format(new Date(read.readAt))}</p>}</div></article>; })}</div></div>;
}
